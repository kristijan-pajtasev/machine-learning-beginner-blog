import React from 'react';
import './Loading.css';

const Loading = () => {
    return (
        <div className="Loading" >
            Under construction
        </div>
    );
};

export default Loading;